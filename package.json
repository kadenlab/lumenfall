{
  "name": "lumenfall",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "vite standalone --host 127.0.0.1",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "vite preview --outDir docs --host 127.0.0.1"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.0.13"
  }
}
