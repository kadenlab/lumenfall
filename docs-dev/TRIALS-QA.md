# 灯の試練 検証記録

## 自動テストとビルド

- npm test：38件成功（既存23件＋試練15件）。
- npm run build：ホスト版とGitHub Pages用静的版が成功。
- 検証項目：未クリア旧記録の禁止、クリア済み旧記録の自動移行、正式フラグ優先、通知の一度きり保存、所持品・HP・章進行保持、順次解放、重複報酬禁止、2枠装備、重複装備禁止、各補正、リジェネ上限・HP0・無癒での禁止、灯0の敗北、霧の3ターン表示、3連戦、Lv8・回避なしで全5試練の勝利。
- ゲーム部分のTypeScriptエラーはなし。全体tscには従来のホスト型cloudflare:workers / Fetcher / D1Databaseの未解決が残る。

## 旧セーブの実操作（9月7日）

試練項目を一切含まないversion 2の仮データを検証用ページから読み込み。実ユーザーの保存領域には接続していない。

未クリア記録：メニューの灯の試練は無効。HP201、MP65、所持金321、薬草7、星の雫4を維持。

クリア済み記録：解放通知を表示し、そのまま祭壇へ移動。保存・再読み込み後は通知を再表示せず、記録と報酬を維持。

## 全試練の実操作

- 第一（PC幅）：対象を選び3 Wave撃破。13ターン、残HP60、灯14でクリアし灯守の指輪を取得。
- 第二（390×740）：増援を確認し15ターン生存。残HP41、ペンダント取得。
- 第三（390×740）：灯を掲げる、星火、防御で11ターンクリア。残HP49、護符取得。
- 装備画面でペンダントと護符を2枠に装備。重複ボタンが無効になり、HP上限268→294、現在HP201は変化しないことを確認。
- 第四（390×740）：回復コマンドが無効。星火の威力81→87、HPを引き継ぎ3連戦を8ターンでクリア。残HP185、耳飾り取得。
- 各クリア後に本編のHP201・MP65へ戻り、次の試練が解放されることを確認。

9月8日の再開時に検証ブラウザが初期化されたため、上記の実プレイ結果と同等の第四クリア記録を検証用ページから復元し、第五の試練から継続。検証用ページ・データは配布物から除外。

## 表示・操作と限界

PC幅と390×740のiframeで確認。スマホ幅は同じスティックにpointerを送るための検証用CSSを適用。iOS/Android実機のOS操作は未確認。

このブラウザではWebGLが無効で、Canvas互換描画を使用。GPUのBloom・反射・影と実機60fpsは未確認。ブラウザ拡張由来のエラーおよびWebGL作成失敗はゲーム進行の例外と分けて扱う。

初回共通バンドルは約622kB（gzip約166kB）。試練エリアとマップは遅延ロードし、専用水面や毎フレームの処理は追加していない。

## 最終確認（9月8日）

- 第五を実操作で20ターンクリア。第二形態・予告・灯防衛・欠片回収を使用し、残HP43・灯33で永灯の首飾りを取得。全5試練を実操作でクリア済み。
- 再読み込みで全クリア、5報酬、ベスト記録、2枠装備を維持。解放通知の再表示なし。
- 永灯＋霧灯装備で第五を再挑戦。294HPから18ダメージ後、次ターン開始時に8回復して284HP。第四では防御後289HPとなり、リジェネが無効であることを実測。
- 撤退・霧港への復帰で本編HP201/294・MP65/86へ復元。検証記録には港の初回入場記録がないため、港の導入会話を表示。
- スティックのドラッグと解放時のリセットを確認。9コマンド時にMPへ重なるモバイル表示を修正し、390×740で表示を再確認。
- 最終CSS修正後のビルド成功、最終npm testは38/38成功。今回Chapter 1・2の全編実プレイは再実施せず、既存の回帰テストと港への復帰で検証。
- 確認中にゲーム処理の例外は検出せず。WebGL不可およびブラウザ拡張の環境エラーは残る。

## 試練表示・第四調整（2026-09-08追記）

- npm test：41件成功。撃破済みの名前・HP・選択肢が消えること、第四の敵置換とHP引継ぎ、剣連打の敗北と防御を交えた勝利を追加検証。
- 最終ゲームコードのnpm run build成功。
- 390×740の実操作で第二を開始。兵→弓手→最初の増援を倒した後、8ターン終了時の第四の敵の姿・HPカードを確認。倒した敵の表示は消え、15ターンで正常クリア。
- 再読み込みでクリア記録・所持品を保持。
- 検証ブラウザはGPUコンテキストを利用できず、同じ3Dシーンを描く互換描画で検証。WebGLの発光や実機性能は未検証。
- 最新ビルドの第四を、Lv8・アクセサリー／剣／防具なしで開始。予告された3行動ごとの強攻撃を防御し、それ以外は星火を使って16ターンで正常クリア。回復コマンドは無効、Wave間でHP引継ぎを確認。
