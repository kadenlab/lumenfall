import {progress} from '../../systems/progression.ts';
import type {GameState} from '../../systems/state.ts';
export const names=['Start','Village Unlocked','Old City Unlocked','Cathedral Unlocked','Before Aurel','Cleared'];
export function apply(s:GameState,name:string){const p=progress(s.campaign);if(name==='Start')return 0;Object.assign(p.flags,{traveler:true,waylight:true});if(!p.defeatedEnemies.includes('frost'))p.defeatedEnemies.push('frost');if(name==='Village Unlocked')return 1;Object.assign(p.flags,{history:true,smith:true,watcher:true});if(!p.defeatedEnemies.includes('warden'))p.defeatedEnemies.push('warden');if(!p.bosses.includes('warden'))p.bosses.push('warden');p.quests['third-light']='old-city';if(name==='Old City Unlocked')return 2;p.flags.evidence=true;for(const id of ['soldier','shade'])if(!p.defeatedEnemies.includes(id))p.defeatedEnemies.push(id);if(name==='Cathedral Unlocked')return 3;Object.assign(p.flags,{record:true,'hearth-west':true,'hearth-east':true});if(!p.defeatedEnemies.includes('hunter'))p.defeatedEnemies.push('hunter');if(name==='Cleared'){Object.assign(p.flags,{rekindled:true,cityLights:3,worldMap:true,endingComplete:true,nextLight:'silent-ring'});p.defeatedEnemies.push('aurel');p.bosses.push('aurel');p.quests['third-light']='complete'}return name==='Cleared'?2:4}
