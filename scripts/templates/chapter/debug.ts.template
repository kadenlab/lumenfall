import type {GameState} from '../../systems/state.ts';
import {progress} from '../../systems/progression.ts';
export const names=['Start','Cleared'];
export const mapGates:Record<number,string>={};
export function apply(s:GameState,name:string){
 const p=progress(s.campaign);
 // Fill in boss/event/quest prerequisites once the story is implemented.
 if(name==='Cleared')p.completed=true;
 return 0;
}
export async function placements(){return {placements:{0:[]}}}
